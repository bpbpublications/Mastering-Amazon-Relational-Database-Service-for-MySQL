vi describe_MySQL_minor_version.sh#!/bin/bash# Usage functionusage() {    echo "Usage: $0 --engine-version <version> [--region <region>]"    exit 1}# Parse command-line optionswhile [[ $# -gt 0 ]]; do    case $1 in        --engine-version)            ENGINE_VERSION=$2            shift            ;;        --region)            REGION=$2            shift            ;;        *)            echo "Invalid option: $1"            usage            ;;    esac    shiftdone# Check if required parameters are providedif [[ -z $ENGINE_VERSION ]]; then    echo "Error: Engine version (--engine-version) is required."    usagefi# Set default region if not providedREGION=${REGION:-us-west-2}# Describe DB engine versionaws rds describe-db-engine-versions \    --default-only \    --engine mysql \    --engine-version $ENGINE_VERSION \    --region $REGION \    --query "*[].{Engine:Engine,EngineVersion:EngineVersion}"# Upgrade an RDS instance to a supported MySQL versionaws rds modify-db-instance \  --db-instance-identifier <instance_identifier> \  --engine-version <new_mysql_version>vi upgrade_rds.sh#!/bin/bash# Function to display the usage messageusage() {    echo "Usage: $0 --db-instance-identifier <instance_identifier> --engine-version <new_mysql_version>"    exit 1}# Check if required parameters are providedif [ $# -lt 4 ]; then    usagefi# Parse command-line argumentswhile [ $# -gt 0 ]; do    case "$1" in        --db-instance-identifier)            db_instance_identifier="$2"            shift 2;;        --engine-version)            engine_version="$2"            shift 2;;        *)            usage;;    esacdone# Check if the required parameters are providedif [ -z "$db_instance_identifier" ] || [ -z "$engine_version" ]; then    usagefi# Upgrade RDS instanceaws rds modify-db-instance \    --db-instance-identifier "$db_instance_identifier" \    --engine-version "$engine_version" \    --apply-immediately